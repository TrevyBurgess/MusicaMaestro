<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CyberFeedForward.MusicaMaestro.Views.MusicSynthesisView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                x:Name="QueryTextBox"
                AutomationProperties.Name="AI prompt"
                Header="AI prompt"
                PlaceholderText="Describe the music you want..."
                Text="{x:Bind ViewModel.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                MinHeight="80"
                MaxHeight="200" />
            <Button
                x:Name="GenerateButton"
                Grid.Column="1"
                AutomationProperties.Name="Generate music with AI"
                Command="{x:Bind ViewModel.GenerateCommand, Mode=OneWay}"
                Content="Generate"
                VerticalAlignment="Bottom" />
        </Grid>

        <ProgressRing
            Grid.Row="2"
            IsActive="{x:Bind ViewModel.IsGenerating, Mode=OneWay}" />

        <ScrollViewer Grid.Row="3">
            <StackPanel Spacing="8">
                <TextBlock
                    Foreground="Red"
                    Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                    TextWrapping="Wrap" />
                <TextBlock
                    Text="{x:Bind ViewModel.Response, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
